/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  // i18n: {
  //   locales: ['en', 'uk'],
  //   defaultLocale: 'en',
  // },
};

module.exports = nextConfig;
